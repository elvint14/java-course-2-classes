String
    - recap
    - Types of data types (primitive and non-primitive(reference))
    - Char array and understanding String
    - String under the hood (uses char array etc.)
    - Methods of String class (some)
        - toLowerCase() & toUpperCase()
        - substring() & trim()
        - indexOf(String s) & indexOf(int i)
        - split(), replace(), length(), concat()
